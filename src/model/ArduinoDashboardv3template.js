/**
 * Arduino IoT Cloud API
 * Provides a set of endpoints to manage Arduino IoT Cloud **Devices**, **Things**, **Properties** and **Timeseries**. This API can be called just with any HTTP Client, or using one of these clients:  * [Javascript NPM package](https://www.npmjs.com/package/@arduino/arduino-iot-client)  * [Python PYPI Package](https://pypi.org/project/arduino-iot-client/)  * [Golang Module](https://github.com/arduino/iot-client-go)
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ArduinoPagevariable from './ArduinoPagevariable';
import ArduinoWidgetv3template from './ArduinoWidgetv3template';

/**
 * The ArduinoDashboardv3template model module.
 * @module model/ArduinoDashboardv3template
 * @version 3.1.1
 */
class ArduinoDashboardv3template {
    /**
     * Constructs a new <code>ArduinoDashboardv3template</code>.
     * ArduinoDashboardv3template media type (default view)
     * @alias module:model/ArduinoDashboardv3template
     * @param name {String} The friendly name of the dashboard
     */
    constructor(name) { 
        
        ArduinoDashboardv3template.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>ArduinoDashboardv3template</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ArduinoDashboardv3template} obj Optional instance to populate.
     * @return {module:model/ArduinoDashboardv3template} The populated <code>ArduinoDashboardv3template</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ArduinoDashboardv3template();

            if (data.hasOwnProperty('cover_image')) {
                obj['cover_image'] = ApiClient.convertToType(data['cover_image'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('pages')) {
                obj['pages'] = ApiClient.convertToType(data['pages'], [ArduinoPagevariable]);
            }
            if (data.hasOwnProperty('widgets')) {
                obj['widgets'] = ApiClient.convertToType(data['widgets'], [ArduinoWidgetv3template]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ArduinoDashboardv3template</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ArduinoDashboardv3template</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ArduinoDashboardv3template.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cover_image'] && !(typeof data['cover_image'] === 'string' || data['cover_image'] instanceof String)) {
            throw new Error("Expected the field `cover_image` to be a primitive type in the JSON string but got " + data['cover_image']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['pages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pages'])) {
                throw new Error("Expected the field `pages` to be an array in the JSON data but got " + data['pages']);
            }
            // validate the optional field `pages` (array)
            for (const item of data['pages']) {
                ArduinoPagevariable.validateJSON(item);
            };
        }
        if (data['widgets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['widgets'])) {
                throw new Error("Expected the field `widgets` to be an array in the JSON data but got " + data['widgets']);
            }
            // validate the optional field `widgets` (array)
            for (const item of data['widgets']) {
                ArduinoWidgetv3template.validateJSON(item);
            };
        }

        return true;
    }


}

ArduinoDashboardv3template.RequiredProperties = ["name"];

/**
 * The cover image of the dashboard
 * @member {String} cover_image
 */
ArduinoDashboardv3template.prototype['cover_image'] = undefined;

/**
 * The friendly ID of the dashboard
 * @member {String} id
 */
ArduinoDashboardv3template.prototype['id'] = undefined;

/**
 * The friendly name of the dashboard
 * @member {String} name
 */
ArduinoDashboardv3template.prototype['name'] = undefined;

/**
 * ArduinoPagevariableCollection is the media type for an array of ArduinoPagevariable (default view)
 * @member {Array.<module:model/ArduinoPagevariable>} pages
 */
ArduinoDashboardv3template.prototype['pages'] = undefined;

/**
 * ArduinoWidgetv3templateCollection is the media type for an array of ArduinoWidgetv3template (default view)
 * @member {Array.<module:model/ArduinoWidgetv3template>} widgets
 */
ArduinoDashboardv3template.prototype['widgets'] = undefined;






export default ArduinoDashboardv3template;

