/**
 * Arduino IoT Cloud API
 * Provides a set of endpoints to manage Arduino IoT Cloud **Devices**, **Things**, **Properties** and **Timeseries**. This API can be called just with any HTTP Client, or using one of these clients:  * [Javascript NPM package](https://www.npmjs.com/package/@arduino/arduino-iot-client)  * [Python PYPI Package](https://pypi.org/project/arduino-iot-client/)  * [Golang Module](https://github.com/arduino/iot-client-go)
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ArduinoDashboardowner from './ArduinoDashboardowner';
import ArduinoDashboardshare from './ArduinoDashboardshare';
import ArduinoPagevariable from './ArduinoPagevariable';
import ArduinoWidgetv3 from './ArduinoWidgetv3';

/**
 * The ArduinoDashboardv3 model module.
 * @module model/ArduinoDashboardv3
 * @version 3.1.3
 */
class ArduinoDashboardv3 {
    /**
     * Constructs a new <code>ArduinoDashboardv3</code>.
     * Dashboard is a collection of widgets (default view)
     * @alias module:model/ArduinoDashboardv3
     * @param id {String} The friendly name of the dashboard
     * @param name {String} The friendly name of the dashboard
     * @param updatedAt {Date} Last update date
     */
    constructor(id, name, updatedAt) { 
        
        ArduinoDashboardv3.initialize(this, id, name, updatedAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, updatedAt) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['updated_at'] = updatedAt;
    }

    /**
     * Constructs a <code>ArduinoDashboardv3</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ArduinoDashboardv3} obj Optional instance to populate.
     * @return {module:model/ArduinoDashboardv3} The populated <code>ArduinoDashboardv3</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ArduinoDashboardv3();

            if (data.hasOwnProperty('cover_image')) {
                obj['cover_image'] = ApiClient.convertToType(data['cover_image'], 'String');
            }
            if (data.hasOwnProperty('created_by')) {
                obj['created_by'] = ArduinoDashboardowner.constructFromObject(data['created_by']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('organization_id')) {
                obj['organization_id'] = ApiClient.convertToType(data['organization_id'], 'String');
            }
            if (data.hasOwnProperty('pages')) {
                obj['pages'] = ApiClient.convertToType(data['pages'], [ArduinoPagevariable]);
            }
            if (data.hasOwnProperty('shared_by')) {
                obj['shared_by'] = ArduinoDashboardshare.constructFromObject(data['shared_by']);
            }
            if (data.hasOwnProperty('shared_with')) {
                obj['shared_with'] = ApiClient.convertToType(data['shared_with'], [ArduinoDashboardshare]);
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('widgets')) {
                obj['widgets'] = ApiClient.convertToType(data['widgets'], [ArduinoWidgetv3]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ArduinoDashboardv3</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ArduinoDashboardv3</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ArduinoDashboardv3.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cover_image'] && !(typeof data['cover_image'] === 'string' || data['cover_image'] instanceof String)) {
            throw new Error("Expected the field `cover_image` to be a primitive type in the JSON string but got " + data['cover_image']);
        }
        // validate the optional field `created_by`
        if (data['created_by']) { // data not null
          ArduinoDashboardowner.validateJSON(data['created_by']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['organization_id'] && !(typeof data['organization_id'] === 'string' || data['organization_id'] instanceof String)) {
            throw new Error("Expected the field `organization_id` to be a primitive type in the JSON string but got " + data['organization_id']);
        }
        if (data['pages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pages'])) {
                throw new Error("Expected the field `pages` to be an array in the JSON data but got " + data['pages']);
            }
            // validate the optional field `pages` (array)
            for (const item of data['pages']) {
                ArduinoPagevariable.validateJSON(item);
            };
        }
        // validate the optional field `shared_by`
        if (data['shared_by']) { // data not null
          ArduinoDashboardshare.validateJSON(data['shared_by']);
        }
        if (data['shared_with']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['shared_with'])) {
                throw new Error("Expected the field `shared_with` to be an array in the JSON data but got " + data['shared_with']);
            }
            // validate the optional field `shared_with` (array)
            for (const item of data['shared_with']) {
                ArduinoDashboardshare.validateJSON(item);
            };
        }
        if (data['widgets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['widgets'])) {
                throw new Error("Expected the field `widgets` to be an array in the JSON data but got " + data['widgets']);
            }
            // validate the optional field `widgets` (array)
            for (const item of data['widgets']) {
                ArduinoWidgetv3.validateJSON(item);
            };
        }

        return true;
    }


}

ArduinoDashboardv3.RequiredProperties = ["id", "name", "updated_at"];

/**
 * The cover image of the dashboard
 * @member {String} cover_image
 */
ArduinoDashboardv3.prototype['cover_image'] = undefined;

/**
 * @member {module:model/ArduinoDashboardowner} created_by
 */
ArduinoDashboardv3.prototype['created_by'] = undefined;

/**
 * The friendly name of the dashboard
 * @member {String} id
 */
ArduinoDashboardv3.prototype['id'] = undefined;

/**
 * The friendly name of the dashboard
 * @member {String} name
 */
ArduinoDashboardv3.prototype['name'] = undefined;

/**
 * Id of the organization the dashboard belongs to
 * @member {String} organization_id
 */
ArduinoDashboardv3.prototype['organization_id'] = undefined;

/**
 * ArduinoPagevariableCollection is the media type for an array of ArduinoPagevariable (default view)
 * @member {Array.<module:model/ArduinoPagevariable>} pages
 */
ArduinoDashboardv3.prototype['pages'] = undefined;

/**
 * @member {module:model/ArduinoDashboardshare} shared_by
 */
ArduinoDashboardv3.prototype['shared_by'] = undefined;

/**
 * ArduinoDashboardshareCollection is the media type for an array of ArduinoDashboardshare (default view)
 * @member {Array.<module:model/ArduinoDashboardshare>} shared_with
 */
ArduinoDashboardv3.prototype['shared_with'] = undefined;

/**
 * Last update date
 * @member {Date} updated_at
 */
ArduinoDashboardv3.prototype['updated_at'] = undefined;

/**
 * ArduinoWidgetv3Collection is the media type for an array of ArduinoWidgetv3 (default view)
 * @member {Array.<module:model/ArduinoWidgetv3>} widgets
 */
ArduinoDashboardv3.prototype['widgets'] = undefined;






export default ArduinoDashboardv3;

