/**
 * Arduino IoT Cloud API
 * Provides a set of endpoints to manage Arduino IoT Cloud **Devices**, **Things**, **Properties** and **Timeseries**. This API can be called just with any HTTP Client, or using one of these clients:  * [Javascript NPM package](https://www.npmjs.com/package/@arduino/arduino-iot-client)  * [Python PYPI Package](https://pypi.org/project/arduino-iot-client/)  * [Golang Module](https://github.com/arduino/iot-client-go)
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PropertyDefinition from './PropertyDefinition';

/**
 * The PropertyValues model module.
 * @module model/PropertyValues
 * @version 3.1.1
 */
class PropertyValues {
    /**
     * Constructs a new <code>PropertyValues</code>.
     * @alias module:model/PropertyValues
     * @param properties {Array.<module:model/PropertyDefinition>} The set of properties to publish
     */
    constructor(properties) { 
        
        PropertyValues.initialize(this, properties);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, properties) { 
        obj['properties'] = properties;
        obj['to_device'] = false;
    }

    /**
     * Constructs a <code>PropertyValues</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PropertyValues} obj Optional instance to populate.
     * @return {module:model/PropertyValues} The populated <code>PropertyValues</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PropertyValues();

            if (data.hasOwnProperty('properties')) {
                obj['properties'] = ApiClient.convertToType(data['properties'], [PropertyDefinition]);
            }
            if (data.hasOwnProperty('to_device')) {
                obj['to_device'] = ApiClient.convertToType(data['to_device'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PropertyValues</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PropertyValues</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PropertyValues.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['properties']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['properties'])) {
                throw new Error("Expected the field `properties` to be an array in the JSON data but got " + data['properties']);
            }
            // validate the optional field `properties` (array)
            for (const item of data['properties']) {
                PropertyDefinition.validateJSON(item);
            };
        }

        return true;
    }


}

PropertyValues.RequiredProperties = ["properties"];

/**
 * The set of properties to publish
 * @member {Array.<module:model/PropertyDefinition>} properties
 */
PropertyValues.prototype['properties'] = undefined;

/**
 * Handle data direction, simulating data from or to device (default false - data sent by device)
 * @member {Boolean} to_device
 * @default false
 */
PropertyValues.prototype['to_device'] = false;






export default PropertyValues;

