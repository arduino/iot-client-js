/**
 * Arduino IoT Cloud API
 * Provides a set of endpoints to manage Arduino IoT Cloud **Devices**, **Things**, **Properties** and **Timeseries**. This API can be called just with any HTTP Client, or using one of these clients:  * [Javascript NPM package](https://www.npmjs.com/package/@arduino/arduino-iot-client)  * [Python PYPI Package](https://pypi.org/project/arduino-iot-client/)  * [Golang Module](https://github.com/arduino/iot-client-go)
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Pagepayload from './Pagepayload';
import Widgetv3 from './Widgetv3';

/**
 * The Dashboardv3 model module.
 * @module model/Dashboardv3
 * @version 3.1.2
 */
class Dashboardv3 {
    /**
     * Constructs a new <code>Dashboardv3</code>.
     * DashboardV3Payload describes a dashboard
     * @alias module:model/Dashboardv3
     */
    constructor() { 
        
        Dashboardv3.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['soft_deleted'] = false;
    }

    /**
     * Constructs a <code>Dashboardv3</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Dashboardv3} obj Optional instance to populate.
     * @return {module:model/Dashboardv3} The populated <code>Dashboardv3</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Dashboardv3();

            if (data.hasOwnProperty('cover_image')) {
                obj['cover_image'] = ApiClient.convertToType(data['cover_image'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('pages')) {
                obj['pages'] = ApiClient.convertToType(data['pages'], [Pagepayload]);
            }
            if (data.hasOwnProperty('soft_deleted')) {
                obj['soft_deleted'] = ApiClient.convertToType(data['soft_deleted'], 'Boolean');
            }
            if (data.hasOwnProperty('widgets')) {
                obj['widgets'] = ApiClient.convertToType(data['widgets'], [Widgetv3]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Dashboardv3</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Dashboardv3</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['cover_image'] && !(typeof data['cover_image'] === 'string' || data['cover_image'] instanceof String)) {
            throw new Error("Expected the field `cover_image` to be a primitive type in the JSON string but got " + data['cover_image']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['pages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pages'])) {
                throw new Error("Expected the field `pages` to be an array in the JSON data but got " + data['pages']);
            }
            // validate the optional field `pages` (array)
            for (const item of data['pages']) {
                Pagepayload.validateJSON(item);
            };
        }
        if (data['widgets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['widgets'])) {
                throw new Error("Expected the field `widgets` to be an array in the JSON data but got " + data['widgets']);
            }
            // validate the optional field `widgets` (array)
            for (const item of data['widgets']) {
                Widgetv3.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The cover image of the dashboard
 * @member {String} cover_image
 */
Dashboardv3.prototype['cover_image'] = undefined;

/**
 * The friendly name of the dashboard
 * @member {String} name
 */
Dashboardv3.prototype['name'] = undefined;

/**
 * List of sub-pages
 * @member {Array.<module:model/Pagepayload>} pages
 */
Dashboardv3.prototype['pages'] = undefined;

/**
 * If false, restore the thing from the soft deletion
 * @member {Boolean} soft_deleted
 * @default false
 */
Dashboardv3.prototype['soft_deleted'] = false;

/**
 * Widgets attached to this dashboard
 * @member {Array.<module:model/Widgetv3>} widgets
 */
Dashboardv3.prototype['widgets'] = undefined;






export default Dashboardv3;

