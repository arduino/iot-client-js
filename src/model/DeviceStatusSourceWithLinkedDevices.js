/**
 * Arduino IoT Cloud API
 * Provides a set of endpoints to manage Arduino IoT Cloud **Devices**, **Things**, **Properties** and **Timeseries**. This API can be called just with any HTTP Client, or using one of these clients:  * [Javascript NPM package](https://www.npmjs.com/package/@arduino/arduino-iot-client)  * [Python PYPI Package](https://pypi.org/project/arduino-iot-client/)  * [Golang Module](https://github.com/arduino/iot-client-go)
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ArduinoLinkedDevice from './ArduinoLinkedDevice';

/**
 * The DeviceStatusSourceWithLinkedDevices model module.
 * @module model/DeviceStatusSourceWithLinkedDevices
 * @version 3.0.0
 */
class DeviceStatusSourceWithLinkedDevices {
    /**
     * Constructs a new <code>DeviceStatusSourceWithLinkedDevices</code>.
     * Device_status_source_with_linked_devices media type (default view)
     * @alias module:model/DeviceStatusSourceWithLinkedDevices
     * @param criteria {String} The criteria of the trigger, could be INCLUDE or EXCLUDE
     */
    constructor(criteria) { 
        
        DeviceStatusSourceWithLinkedDevices.initialize(this, criteria);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, criteria) { 
        obj['criteria'] = criteria;
    }

    /**
     * Constructs a <code>DeviceStatusSourceWithLinkedDevices</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeviceStatusSourceWithLinkedDevices} obj Optional instance to populate.
     * @return {module:model/DeviceStatusSourceWithLinkedDevices} The populated <code>DeviceStatusSourceWithLinkedDevices</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeviceStatusSourceWithLinkedDevices();

            if (data.hasOwnProperty('criteria')) {
                obj['criteria'] = ApiClient.convertToType(data['criteria'], 'String');
            }
            if (data.hasOwnProperty('grace_period_offline')) {
                obj['grace_period_offline'] = ApiClient.convertToType(data['grace_period_offline'], 'Number');
            }
            if (data.hasOwnProperty('grace_period_online')) {
                obj['grace_period_online'] = ApiClient.convertToType(data['grace_period_online'], 'Number');
            }
            if (data.hasOwnProperty('linked_devices')) {
                obj['linked_devices'] = ApiClient.convertToType(data['linked_devices'], [ArduinoLinkedDevice]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeviceStatusSourceWithLinkedDevices</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeviceStatusSourceWithLinkedDevices</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DeviceStatusSourceWithLinkedDevices.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['criteria'] && !(typeof data['criteria'] === 'string' || data['criteria'] instanceof String)) {
            throw new Error("Expected the field `criteria` to be a primitive type in the JSON string but got " + data['criteria']);
        }
        if (data['linked_devices']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['linked_devices'])) {
                throw new Error("Expected the field `linked_devices` to be an array in the JSON data but got " + data['linked_devices']);
            }
            // validate the optional field `linked_devices` (array)
            for (const item of data['linked_devices']) {
                ArduinoLinkedDevice.validateJSON(item);
            };
        }

        return true;
    }


}

DeviceStatusSourceWithLinkedDevices.RequiredProperties = ["criteria"];

/**
 * The criteria of the trigger, could be INCLUDE or EXCLUDE
 * @member {String} criteria
 */
DeviceStatusSourceWithLinkedDevices.prototype['criteria'] = undefined;

/**
 * The amount of seconds the trigger will wait before considering a matching device as offline
 * @member {Number} grace_period_offline
 */
DeviceStatusSourceWithLinkedDevices.prototype['grace_period_offline'] = undefined;

/**
 * The amount of seconds the trigger will wait before considering a matching device as online
 * @member {Number} grace_period_online
 */
DeviceStatusSourceWithLinkedDevices.prototype['grace_period_online'] = undefined;

/**
 * A list of devices the trigger is associated to
 * @member {Array.<module:model/ArduinoLinkedDevice>} linked_devices
 */
DeviceStatusSourceWithLinkedDevices.prototype['linked_devices'] = undefined;






export default DeviceStatusSourceWithLinkedDevices;

